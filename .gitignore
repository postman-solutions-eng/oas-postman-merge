# dependencies
node_modules/
npm-debug.log*

# local environment/secrets
.env
*.env
.env.local

# generated artifacts (cleanup handles these, but good to ignore too)
ref/
*.merged.json
CHANGELOG.*.md

# user files (keep their private specs/collections out of git)  
# NOTE: demo files (demo-*.yaml, working.json) are NOT ignored - they're part of the demo
openapi/my-*
openapi/user-*
openapi/test-*
collections/my-*
collections/user-*  
collections/test-*
config/my-*
config/user-*
config/test-*

# OS cruft
.DS_Store
Thumbs.db

# editor cruft
*.swp
*.swo
.idea/
.vscode/
