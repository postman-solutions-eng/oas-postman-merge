## Collection Changes

### Added
- DELETE {{baseUrl}}/sites/:siteId — **Delete Site by ID** (Tableau REST API / Sites / sites / {siteId})
- PUT {{baseUrl}}/sites/:siteId — **Update Site** (Tableau REST API / Sites / sites / {siteId})
- GET {{baseUrl}}/sites/:siteId/settings — **Get Site Settings** (Tableau REST API / Sites / sites / {siteId} / settings)
- PUT {{baseUrl}}/sites/:siteId/settings — **Update Site Settings** (Tableau REST API / Sites / sites / {siteId} / settings)

### Retired (not deleted)
- DELETE {{baseUrl}}/sites/:site-id — **Delete Site (by ID) (Server-only)** (Tableau REST API / Sites)
- DELETE {{baseUrl}}/sites/:site-name — **Delete Site (by Site Name) (Server-only)** (Tableau REST API / Sites)
- DELETE {{baseUrl}}/sites/:content-url — **Delete Site (by Content URL) (Server-only)** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-id/dataAccelerationReport — **Get Data Acceleration Report for a Site (Server-only)** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-id/settings/embedding — **Get Embedding Settings for a Site** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-id/content/recent — **Get Recently Viewed for Site** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-id — **Query Site (by ID)** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-name — **Query Site (by Site Name)** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:content-url — **Query Site (by Content URL)** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-id/views — **Query Views for Site** (Tableau REST API / Sites)
- GET {{baseUrl}}/sites/:site-id/site-auth-configurations — **List Auth Configurations for Site** (Tableau REST API / Sites)
- PUT {{baseUrl}}/sites/:site-id/settings/embedding — **Update Embedding Settings for a site** (Tableau REST API / Sites)
- PUT {{baseUrl}}/sites/:site-id — **Update Site** (Tableau REST API / Sites)

