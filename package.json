{
  "name": "postman-merge-demo",
  "private": true,
  "scripts": {
    "merge": "node scripts/merge.js --config config/merge.config.yaml --working collections/working.json --refdir ref --out collections/working.merged.json",
    "changelog": "node scripts/changelog.js --before collections/working.json --after collections/working.merged.json --out CHANGELOG.md"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "js-yaml": "^4.1.0",
    "postman-collection": "^5.2.0",
    "yargs": "^16.2.0"
  }
}
